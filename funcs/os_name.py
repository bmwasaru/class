def check_os_name(operating_system):
	# check the operating system name
	if operating_system == "posix":
		print("You are running a linux operation system")
	if operating_system == "nt":
		print("You are running a Windows NT operating system")
